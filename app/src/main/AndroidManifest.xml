<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Lists the apps with a launcher icon for the per-app switches. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

    <application
        android:allowBackup="false"
        android:enableOnBackInvokedCallback="true"
        tools:targetApi="33"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Ownvoice">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- The keyboard stays down on the practice chat, so its bubble and buttons show; its field has focus for the bubble. -->
        <activity
            android:name=".SetupActivity"
            android:exported="false"
            android:windowSoftInputMode="stateAlwaysHidden|adjustResize" />

        <activity
            android:name=".AppsActivity"
            android:exported="false"
            android:label="Where the bubble shows" />

        <activity
            android:name=".ReadsActivity"
            android:exported="false"
            android:label="What Ownvoice read" />

        <!-- Exported for "Share" of a markdown voice profile; an import only shows what it found until the user adds it. -->
        <activity
            android:name=".VoiceActivity"
            android:exported="true"
            android:label="Your voice">
            <intent-filter android:label="Import to Your voice">
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/markdown" />
                <data android:mimeType="text/x-markdown" />
            </intent-filter>
        </activity>

        <activity
            android:name=".DraftActivity"
            android:excludeFromRecents="true"
            android:exported="false"
            android:taskAffinity="dev.ownvoice.app.drafts"
            android:theme="@style/Ownvoice.Sheet" />

        <activity
            android:name=".RewriteActivity"
            android:excludeFromRecents="true"
            android:exported="true"
            android:theme="@style/Ownvoice.Sheet">
            <intent-filter>
                <action android:name="android.intent.action.PROCESS_TEXT" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>

        <service
            android:name=".OwnvoiceService"
            android:exported="false"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/ownvoice_service" />
        </service>
    </application>
</manifest>
